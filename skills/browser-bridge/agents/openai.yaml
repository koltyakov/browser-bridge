interface:
  display_name: "Browser Bridge"
  short_description: "Operate a scoped Chrome bridge"
  default_prompt: "Use $browser-bridge to inspect a Chrome tab with agent communication enabled through the local bridge with minimal token usage. If browser-bridge is available, do not use Playwright or other browser automation tools; use browser-bridge capabilities only."
